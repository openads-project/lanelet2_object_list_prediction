name: docs

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

jobs:
  docs:
    runs-on: self-hosted
    uses: openads-project/openads-dev-environment/.github/workflows/docs.yml@main
    secrets: inherit
