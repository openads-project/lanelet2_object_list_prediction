name: compose-oci

on:
  push:
    branches:
      - '**'
    tags:
      - '**'
  workflow_dispatch:

jobs:
  compose-oci:
    runs-on: self-hosted
    uses: openads-project/openads-dev-environment/.github/workflows/compose-oci.yml@main
    secrets: inherit
