name: consistency

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * *"

jobs:
  consistency:
    runs-on: self-hosted
    uses: openads-project/openads-dev-environment/.github/workflows/consistency.yml@main
    secrets: inherit
